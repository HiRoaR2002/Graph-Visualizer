# Frontend (TypeScript + React + Vite + Tailwind)

This frontend expects the backend API to be available at `http://localhost:5000`.

## Local dev (Windows PowerShell)

1. Cleanup (if needed)
   ```powershell
   Remove-Item -Recurse -Force node_modules
   Remove-Item -Force package-lock.json
   ```
